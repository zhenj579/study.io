/**
 * @description Fetch string from front end cells
 * @params Contents of a cell
 * @returns mapped dictionary of that content
 */
function getMappedFlashcards(divID){
  var contents= document.getElementById(divID)
}
